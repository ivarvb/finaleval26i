rm -rf .git
git init
git config --local user.name "l.ivarvb"
git config --local user.email "l.ivarvb@github.com"
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ivarvb/finaleval26i
git push -u --force origin master

